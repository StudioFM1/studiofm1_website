<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head.ejs') %>
</head>
<body>
    <%- include('../partials/adminHeader.ejs') %>
    <div class="container-fluid">
        <main class="p-5">
            <div class="table-wrapper">
                <div class="table-title clearfix">
                    <h2 class="float-start">Producer <b>Management</b></h2>
                    <button class="btn btn-secondary float-end mt-1" type="button" data-bs-toggle="modal" data-bs-target="#createUserModal">Add New Producer <i class="fas fa-plus ms-2"></i></button>
                </div>
                <table class="table table-hover">
                    <thead>
                        <tr>
                            <th scope="col">#</th>
                            <th scope="col"><a href="#">Name</a></th>
                            <th scope="col"><a href="#">Role</a></th>
                            <th scope="col"><a href="#">Status</a></th>
                            <th scope="col">Contact</th>
                            <th scope="col">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% if(users && users.length) { %>
                            <% users.forEach((user, index) => { %>
                                <tr>
                                    <th class="fm1-td" scope="row"><%= index + 1 %></th>
                                    <td class="fm1-td"><img class="avatar small-avatar me-3" src="<%= user.profile.avatar %>" alt="avatar"> <%= user.profile.firstName %> <%= user.profile.lastName %></td>
                                    <td class="fm1-td"><%= user.profile.role %></td>
                                    <td class="fm1-td">
                                        <span id="<%= user._id %>_statusIndicator" class="text-<%= user.status.isActive ? (user.status.isVisible ? 'success' : 'warning') : 'danger' %> me-3"><i class="fas fa-circle"></i></span>
                                        <span id="<%= user._id %>_statusTextIndicator"><%= user.status.isActive ? "Active" : "Inactive" %> / <%= user.status.isVisible ? "Visible" : "Hidden" %></span>
                                    </td>
                                    <td class="fm1-td">
                                        Tel: <a href="tel:<%= user.profile.mobilePhone %>"><%= user.profile.mobilePhone %></a>
                                        <br>
                                        Email: <a href="mailto:<%= user.profile.email %>"><%= user.profile.email %></a>
                                    </td>
                                    <td class="fm1-td">
                                        <button class="btn btn-link" type="button" data-bs-toggle="modal" data-bs-target="#userStatusModal" data-user="<%= JSON.stringify(user) %>"><i class="fas fa-cog" data-bs-toggle="tooltip" data-bs-placement="top" title="Settings"></i></button>
                                        <button class="btn btn-link text-danger" type="button" data-bs-toggle="modal" data-bs-target="#deleteUserModal" data-user="<%= JSON.stringify(user) %>"><i class="fas fa-trash-alt" data-bs-toggle="tooltip" data-bs-placement="top" title="Delete"></i></button>
                                    </td>
                                </tr>
                            <% }); %>
                        <% }; %>
                    </tbody>
                </table>
            </div>
        </main>
    </div>
    <%- include('../partials/footer.ejs') %>
    <%- include('../partials/modals/createUser.ejs') %>
    <%- include('../partials/modals/deleteUser.ejs') %>
    <%- include('../partials/modals/userStatus.ejs') %>
    <script src="/js/vendors/bootstrap.bundle.min.js"></script>
    <script src="/js/form.js"></script>
    <script src="/js/producers.js"></script>
    <script>
        /* Add event listeners when content is loaded */
        window.addEventListener('DOMContentLoaded', () => {
            addUserFormEvents();
            addModalEvents();
        });
    </script>
</body>
</html>
